{
	"name": "formatforge",
	"version": "0.1.5",
	"description": "Simple typography and colour formatting for Obsidian \u2014 standalone, and for the Forge plugin family.",
	"main": "main.js",
	"scripts": {
		"dev": "node esbuild.config.mjs",
		"build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
		"test": "vitest run",
		"test:stress": "vitest run"
	},
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^20.11.0",
		"esbuild": "^0.20.0",
		"obsidian": "latest",
		"typescript": "^5.4.0",
		"vitest": "^1.6.1"
	}
}
